apply from: "${project.rootDir}/gradle/android/library.gradle"

android {
    namespace = "core.datasource.market.review"
}

dependencies {
    implementation project(":core:datasource")
    api libs.google.app.review
}