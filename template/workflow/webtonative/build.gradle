apply from: "${project.rootDir}/gradle/android/library.gradle"

android {
    namespace = "workflow.webtonative"
}

dependencies {
    implementation project(":core:ui")
    implementation libs.androidx.webkit
}